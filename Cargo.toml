[package]
name = "rust-tooling-testbed"
version = "0.1.0"
edition = "2024"
description = "A task management library for testing tooling"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
